export const PROMPT_VERSION='btcodehs-v1';
const refs={python:'Python lớp 10: input/print, 4 dấu cách, tối thiểu hai input khác nhau; expected_output là text.',sql:'SQL lớp 11: bắt buộc setup_sql; expected_output là JSON mảng hai chiều; giữ thứ tự ORDER BY.',html:'HTML/CSS lớp 12: expected_output/selector là CSS selector; autograder chỉ kiểm tra thẻ; Description có rubric thẩm mỹ thủ công 30%.'};
const clean=(v,max=12000)=>{const s=String(v||'').trim();if(!s)throw new Error('Yêu cầu không được để trống.');if(s.length>max)throw new Error(`Yêu cầu tối đa ${max} ký tự.`);return s};
export const buildAssignmentPrompt=(input)=>({version:PROMPT_VERSION,system:`Bạn là BTcodehs. Trả duy nhất JSON đúng schema. Description tiếng Việt; Starter Code có gợi ý nhưng không lộ lời giải; Solution ngắn gọn. Có tối thiểu 3 test_kind: normal, boundary, anti_hardcode; test_name không dấu; tổng points bằng max_score. ${input.subject?refs[input.subject]:Object.values(refs).join(' ')}`,user:`Yêu cầu/bài mẫu: ${clean(input.request)}\nMôn: ${input.subject||'tự nhận diện'}\nĐộ khó: ${input.difficulty||'tự chọn'}\nBổ sung: ${String(input.additional_requirements||'').slice(0,2000)}`});
export const buildRepairPrompt=({input,invalidOutput,issues})=>({...buildAssignmentPrompt(input),user:`${buildAssignmentPrompt(input).user}\nSửa JSON sau theo lỗi: ${issues.join('; ')}\nJSON lỗi: ${JSON.stringify(invalidOutput).slice(0,16000)}`});
