import {AIConfigurationError,AIProviderError} from '../../services/aiGateway.js';
export const createGeminiProvider=({apiKey,model,fetchImpl=fetch})=>({async generateStructured({system,user,schema,signal}){if(!apiKey||!model)throw new AIConfigurationError('Thiếu cấu hình Gemini.');const url=`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent`;const res=await fetchImpl(url,{method:'POST',signal,headers:{'x-goog-api-key':apiKey,'Content-Type':'application/json'},body:JSON.stringify({systemInstruction:{parts:[{text:system}]},contents:[{role:'user',parts:[{text:user}]}],generationConfig:{responseMimeType:'application/json',responseSchema:schema}})});if(!res.ok)throw new AIProviderError(`Gemini HTTP ${res.status}`);const data=await res.json();const text=data.candidates?.[0]?.content?.parts?.map(x=>x.text||'').join('')||'{}';return {value:JSON.parse(text),usage:{input_tokens:data.usageMetadata?.promptTokenCount,output_tokens:data.usageMetadata?.candidatesTokenCount},model}}});
