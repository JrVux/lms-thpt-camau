import {AIConfigurationError,AIProviderError} from '../../services/aiGateway.js';
export const createOpenRouterProvider=({apiKey,model,fetchImpl=fetch})=>({async generateStructured({system,user,schema,signal}){if(!apiKey||!model)throw new AIConfigurationError('Thiếu cấu hình OpenRouter.');const res=await fetchImpl('https://openrouter.ai/api/v1/chat/completions',{method:'POST',signal,headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json'},body:JSON.stringify({model,messages:[{role:'system',content:system},{role:'user',content:user}],response_format:{type:'json_schema',json_schema:{name:'assignment_draft',strict:true,schema}},provider:{require_parameters:true}})});if(!res.ok)throw new AIProviderError(`OpenRouter HTTP ${res.status}`);const data=await res.json();return {value:JSON.parse(data.choices?.[0]?.message?.content||'{}'),usage:data.usage||{},model:data.model||model}}});
